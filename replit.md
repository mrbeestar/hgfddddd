# بوت تليجرام متكامل

بوت تليجرام مع لوحة تحكم داخلية للمدير عبر الأزرار الشفافة (Inline Keyboard).

## تشغيل المشروع

- `pnpm --filter @workspace/api-server run dev` — تشغيل الخادم + البوت (منفذ 8080)
- `pnpm run typecheck` — فحص TypeScript
- `pnpm --filter @workspace/db run push` — تطبيق تغييرات قاعدة البيانات

## المتغيرات المطلوبة

- `BOT_TOKEN` — توكن البوت من BotFather (سر)
- `OWNER_ID` — معرف تليجرام للمالك (5070528919)
- `DATABASE_URL` — رابط قاعدة البيانات (تلقائي)

## المكدس التقني

- Node.js 24, TypeScript 5.9
- Telegram Bot: node-telegram-bot-api (polling mode)
- DB: PostgreSQL + Drizzle ORM
- API: Express 5

## بنية المشروع

```
artifacts/api-server/src/
├── bot/
│   ├── index.ts           — البوت الرئيسي + منطق المستخدمين
│   ├── keyboard.ts        — مُحلل صيغة الأزرار الشفافة
│   ├── settings-cache.ts  — كاش الإعدادات (30 ثانية)
│   └── admin/
│       ├── state.ts       — آلة الحالة للمدير
│       ├── menu.ts        — قوائم لوحة التحكم
│       └── panel.ts       — معالجات لوحة التحكم
lib/db/src/schema/
├── users.ts       — جدول المستخدمين
├── settings.ts    — جدول الإعدادات
├── fixed_buttons.ts — الأزرار الثابتة + رسائلها
├── channels.ts    — قنوات الاشتراك الإجباري
└── broadcasts.ts  — سجل الإذاعات
```

## ميزات البوت

### للمدير (المالك فقط)
- لوحة تحكم داخل تليجرام بالأزرار الشفافة
- إحصائيات (مستخدمون، محظورون، إذاعات)
- إذاعة لجميع المستخدمين (نص/صورة/فيديو/صوت/ملف/GIF)
- إدارة المستخدمين (حظر/رفع حظر/عرض قائمة)
- إدارة الأزرار الثابتة (إضافة/حذف/تفعيل/رسائل)
- إدارة قنوات الاشتراك الإجباري
- تخصيص رسالة الترحيب وأزرارها

### للمستخدمين
- تحقق الاشتراك الإجباري في القنوات
- أزرار ثابتة (Reply Keyboard) تُرسل رسائل عند الضغط
- أزرار شفافة (Inline Keyboard) مرتبطة بالرسائل

## صيغة الأزرار الشفافة

```
نص الزر = رابط
نص الزر 1 = رابط 1 + نص الزر 2 = رابط 2
```

## User preferences

- لوحة التحكم داخل تليجرام فقط (لا واجهة ويب)
